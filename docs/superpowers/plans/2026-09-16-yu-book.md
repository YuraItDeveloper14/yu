# The Yu Book Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A beginner's book for Yu in Ukrainian and English — ten written chapters and a reference generated from the core — readable on GitHub and published at `yu-lang.vercel.app/book/`, with every example checked by a test.

**Architecture:** The chapters are Markdown in `docs/book/<lang>/`. A Rust test in `yu-core` runs every fenced `yu` block, feeds it the `input` block above it, compares what it prints with the `text` block below it, and requires `yu-error` blocks to fail with exactly the message the book shows. A Node generator turns the Markdown into static pages that reuse the Studio's tokens, tokenizer and share links; Vite builds those pages next to the Studio.

**Tech Stack:** Rust 1.98, Node 24, Vite 8.3.0, TypeScript 7.0.2, `markdown-it` 15.0.2 (build time only), `@playwright/test` 1.63.0.

## Global Constraints

- Spec: `docs/superpowers/specs/2026-09-16-yu-book-design.md`; where it differs from earlier designs, it wins.
- Branch: `m5-book`. Base: `main` at `d068097`.
- The only new npm package is `markdown-it`, pinned to exactly `15.0.2`, in `devDependencies`.
- **Fenced blocks in a chapter** carry meaning and nothing else may use these words: ` ```yu ` is a program that must run; ` ```yu-error ` is a program that must fail; ` ```input ` right before a program holds the lines the reader types, one per line; ` ```text ` right after a program holds exactly what the program prints (or, after a `yu-error` block, the error as Yu renders it).
- **The book's voice:** write to the reader as «ти» / "you". Short sentences, one idea at a time, no word left unexplained. Every new word of the language appears in an example in the same section. No exclamation marks outside example programs, no filler, no "просто" / "simply".
- **Every chapter** has exactly one `# ` heading, sections under `## `, and ends with «Спробуй сам» / "Try it yourself" holding exactly two exercises, each with its answer inside `<details><summary>Показати відповідь</summary>` / `<summary>Show the answer</summary>` and a `yu` block.
- **The two languages** hold the same chapter ids and the same examples; Ukrainian chapters use Ukrainian keywords and Ukrainian names inside the programs, English chapters use English ones.
- Examples stay at most eight lines and use only words the reader has already met, except when the chapter introduces them.
- The expected output in a `text` block is what the test prints when it disagrees — never guess twice: run `cargo test -p yu-core --test book` and copy the real lines.
- cargo lives in `$HOME/.cargo/bin`: in Git Bash run `export PATH="$HOME/.cargo/bin:$PATH"` first; use `set -o pipefail` whenever output is piped, and never hide a test's exit code behind a pipe.
- Before every commit: `cargo fmt --all`, `cargo clippy --all-targets -- -D warnings`, `cargo test --all`; inside `studio/`: `npm run build` and `npm test`.
- The browser tests run in Playwright's headless shell: `export YU_CHROMIUM="C:/Users/home/AppData/Local/ms-playwright/chromium_headless_shell-1208/chrome-headless-shell-win64/chrome-headless-shell.exe"`. A `playwright test` run empties `test-results/`, so never run it beside the screenshot or GIF scripts.
- Commit with `git -c user.email=y.dmytrenko14@gmail.com -c user.name="Yurii Dmytrenko" commit -m "<message>" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"`, staging explicit paths only.

---

## File Structure

| File | Change | Responsibility |
|---|---|---|
| `crates/yu-core/tests/book.rs` | create | runs every example, checks printed output and error messages, checks the chapters' shape |
| `docs/book/README.md` | create | what the book is, links to both languages |
| `docs/book/uk/01-start.md` … `10-projects.md` | create | the ten written chapters in Ukrainian |
| `docs/book/en/01-start.md` … `10-projects.md` | create | the same ten in English |
| `docs/book/uk/11-words.md`, `docs/book/en/11-words.md` | create (generated) | the reference, written by the generator from the core's library |
| `studio/src/book-tools.ts` | create | pure helpers: slugs, chapter list, search index, code colouring to HTML |
| `studio/scripts/build-book.mjs` | create | reads the Markdown, writes the pages, the reference chapter, the search index, the sitemap |
| `studio/src/book.css` | create | the book's layout on the Studio's tokens |
| `studio/src/book.ts` | create | the page's script: theme, language, search |
| `studio/vite.config.ts` | modify | every generated page becomes a build input |
| `studio/package.json`, `.gitignore` | modify | the `book` script, `markdown-it`, ignoring generated files |
| `studio/src/i18n.ts`, `src/main.ts` | modify | «Книга Yu» in the Help menu |
| `studio/tests/book.test.ts` | create | unit tests for the generator's helpers |
| `studio/tests/book.spec.ts` | create | browser tests for the book pages |
| `.github/workflows/ci.yml` | modify | fails when the generated reference is stale |
| `README.md` | modify | links to the book |

---

### Task 1: The test that reads the book, and the first chapter

**Files:**
- Create: `crates/yu-core/tests/book.rs`
- Create: `docs/book/README.md`, `docs/book/uk/01-start.md`, `docs/book/en/01-start.md`

**Interfaces:**
- Consumes: `yu_core::{Host, Lang, Options, Session}`, `Diagnostic::render(src, lang)`, `Session::drawing()`, `lexer::lex`, `token::TokenKind`, `builtins::Builtin::lookup`.
- Produces: the fenced-block contract every later chapter follows, and the helpers inside the test (`blocks`, `Reader`, `chapters`, `stem`, `speaks`) that later tasks do not touch.

- [ ] **Step 1: Write the failing test**

Create `crates/yu-core/tests/book.rs`:

````rust
//! Every example in the book runs, and prints exactly what the book says it prints.

use std::fs;
use std::path::{Path, PathBuf};

use yu_core::builtins::Builtin;
use yu_core::lexer::lex;
use yu_core::token::TokenKind;
use yu_core::{Host, Lang, Options, Session};

/// One fenced block of a chapter.
struct Block {
    info: String,
    body: String,
    line: usize,
}

/// The fenced blocks of `source`, in order, with the line each one opens on.
fn blocks(source: &str) -> Vec<Block> {
    let mut found = Vec::new();
    let mut lines = source.lines().enumerate();
    while let Some((number, line)) = lines.next() {
        let Some(info) = line.strip_prefix("```") else {
            continue;
        };
        let mut body = String::new();
        for (_, inner) in lines.by_ref() {
            if inner.trim_end() == "```" {
                break;
            }
            body.push_str(inner);
            body.push('\n');
        }
        found.push(Block {
            info: info.trim().to_string(),
            body,
            line: number + 1,
        });
    }
    found
}

/// Answers `запитай` from the book's input block, then with a name.
struct Reader {
    answers: std::vec::IntoIter<String>,
    fallback: &'static str,
    out: Vec<String>,
}

impl Host for Reader {
    fn print(&mut self, line: &str) {
        self.out.push(line.to_string());
    }

    fn ask(&mut self, _prompt: &str) -> Option<String> {
        Some(
            self.answers
                .next()
                .unwrap_or_else(|| self.fallback.to_string()),
        )
    }
}

/// The chapter files of one language, in order.
fn chapters(lang: Lang) -> Vec<PathBuf> {
    let dir = PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .join("../../docs/book")
        .join(lang.pick("uk", "en"));
    let mut files: Vec<PathBuf> = fs::read_dir(&dir)
        .unwrap_or_else(|e| panic!("{}: {e}", dir.display()))
        .map(|entry| entry.expect("a chapter").path())
        .filter(|path| path.extension().is_some_and(|kind| kind == "md"))
        .collect();
    files.sort();
    files
}

fn stem(path: &Path) -> String {
    path.file_stem()
        .expect("a chapter")
        .to_string_lossy()
        .to_string()
}

/// The lines of `source` outside fenced blocks; a `#` comment in an example is not a heading.
fn prose(source: &str) -> Vec<&str> {
    let mut out = Vec::new();
    let mut inside = false;
    for line in source.lines() {
        if line.starts_with("```") {
            inside = !inside;
            continue;
        }
        if !inside {
            out.push(line);
        }
    }
    out
}

/// Every keyword and built-in in `program` is spelled in `lang`; a program that
/// does not even lex (the chapter about mistakes has those) is left alone.
fn speaks(lang: Lang, program: &str, where_: &str) {
    let Ok(tokens) = lex(program) else {
        return;
    };
    for token in &tokens {
        match &token.kind {
            TokenKind::Kw(_) => {
                let spelled = &program[token.span.start..token.span.end];
                assert_eq!(
                    spelled.is_ascii(),
                    lang == Lang::En,
                    "{where_} uses «{spelled}»"
                );
            }
            TokenKind::Name(name) => {
                if let Some(b) = Builtin::lookup(name) {
                    assert_eq!(b.name(lang), name.as_str(), "{where_}");
                }
            }
            _ => {}
        }
    }
}

#[test]
fn every_example_in_the_book_runs() {
    for lang in [Lang::Uk, Lang::En] {
        for path in chapters(lang) {
            let source = fs::read_to_string(&path).expect("a chapter");
            let found = blocks(&source);
            let draws = matches!(stem(&path).as_str(), "07-drawing" | "08-turtle");
            for (index, block) in found.iter().enumerate() {
                if block.info != "yu" && block.info != "yu-error" {
                    continue;
                }
                let where_ = format!("{}:{}", path.display(), block.line);
                let answers: Vec<String> = found[..index]
                    .last()
                    .filter(|earlier| earlier.info == "input")
                    .map(|earlier| earlier.body.lines().map(str::to_string).collect())
                    .unwrap_or_default();
                let expected = found.get(index + 1).filter(|next| next.info == "text");
                let mut host = Reader {
                    answers: answers.into_iter(),
                    fallback: lang.pick("Юрій", "Yurii"),
                    out: Vec::new(),
                };
                let mut session = Session::new(Options {
                    lang,
                    step_budget: Some(200_000),
                    ..Options::default()
                });
                let result = session.run(&block.body, &mut host);
                if block.info == "yu" {
                    match result {
                        Err(e) => panic!("{where_}\n{}", e.render(&block.body, lang)),
                        Ok(_) => {
                            if let Some(text) = expected {
                                assert_eq!(host.out.join("\n"), text.body.trim_end(), "{where_}");
                            }
                            if draws {
                                assert!(!session.drawing().is_empty(), "{where_} draws nothing");
                            }
                        }
                    }
                } else {
                    let text = expected
                        .unwrap_or_else(|| panic!("{where_}: an error example needs its message"));
                    match result {
                        Ok(_) => panic!("{where_}: this example should fail, but it ran"),
                        Err(e) => assert_eq!(
                            e.render(&block.body, lang).trim_end(),
                            text.body.trim_end(),
                            "{where_}"
                        ),
                    }
                }
                speaks(lang, &block.body, &where_);
            }
        }
    }
}

#[test]
fn the_chapters_are_shaped_alike() {
    let uk: Vec<String> = chapters(Lang::Uk).iter().map(|p| stem(p)).collect();
    let en: Vec<String> = chapters(Lang::En).iter().map(|p| stem(p)).collect();
    assert_eq!(uk, en, "the two languages hold different chapters");
    for lang in [Lang::Uk, Lang::En] {
        for path in chapters(lang) {
            let source = fs::read_to_string(&path).expect("a chapter");
            let titles = prose(&source).iter().filter(|line| line.starts_with("# ")).count();
            assert_eq!(titles, 1, "{}", path.display());
            if stem(&path) != "11-words" {
                let exercises = source.matches("<details>").count();
                assert_eq!(exercises, 2, "{} needs two exercises", path.display());
            }
            let found = blocks(&source);
            for (index, block) in found.iter().enumerate() {
                if block.info != "text" {
                    continue;
                }
                let earlier = found[..index]
                    .last()
                    .map(|b| b.info.clone())
                    .unwrap_or_default();
                assert!(
                    earlier == "yu" || earlier == "yu-error",
                    "{}:{} output without an example",
                    path.display(),
                    block.line
                );
            }
        }
    }
}
````

- [ ] **Step 2: Run the tests to see them fail**

Run: `cargo test -p yu-core --test book`
Expected: both tests fail — the folder `docs/book/uk` does not exist yet, so `chapters` panics with that path.

- [ ] **Step 3: Write the first chapter and the book's front page**

Create `docs/book/README.md`:

```markdown
# Книга Yu / The Yu book

Посібник для тих, хто пише першу програму. Читай прямо тут або на сайті, де кожен приклад
відкривається в Студії одним натисканням: [yu-lang.vercel.app/book](https://yu-lang.vercel.app/book/).

A guide for someone writing their first program. Read it here, or on the site where every example
opens in the Studio with one click.

- Українською: [Перша програма](uk/01-start.md) · [Числа й текст](uk/02-values.md) · [Умови](uk/03-conditions.md) · [Цикли](uk/04-loops.md) · [Списки](uk/05-lists.md) · [Функції](uk/06-functions.md) · [Малювання](uk/07-drawing.md) · [Черепашка](uk/08-turtle.md) · [Коли щось не так](uk/09-errors.md) · [Три проєкти](uk/10-projects.md) · [Усі слова](uk/11-words.md)
- In English: [Your first program](en/01-start.md) · [Numbers and text](en/02-values.md) · [Conditions](en/03-conditions.md) · [Loops](en/04-loops.md) · [Lists](en/05-lists.md) · [Functions](en/06-functions.md) · [Drawing](en/07-drawing.md) · [The turtle](en/08-turtle.md) · [When something goes wrong](en/09-errors.md) · [Three projects](en/10-projects.md) · [All words](en/11-words.md)
```

Create `docs/book/uk/01-start.md` with this shape, written in the book's voice:

- `# Перша програма`
- An opening paragraph: Yu runs in the browser at yu-lang.vercel.app, nothing to install; the editor is on the left, the picture and the terminal on the right.
- `## Скажи щось` — the first program and what each part means:

```yu
скажи("Привіт, світе!")
```

```text
Привіт, світе!
```

- `## Кілька рядків` — programs run top to bottom, and `#` starts a comment:

```yu
# Цей рядок Yu не виконує
скажи("Мене звати Юрій")
скажи("Мені 15 років")
```

```text
Мене звати Юрій
Мені 15 років
```

- `## Yu вміє рахувати` — several values in one `скажи`, separated by commas:

```yu
скажи("2 + 2 =", 2 + 2)
```

```text
2 + 2 = 4
```

- `## Спробуй сам` with exactly two exercises:
  1. «Напиши програму, яка друкує два рядки про тебе» — answer:

```yu
скажи("Мене звати Оля")
скажи("Я з Києва")
```

  2. «Нехай Yu порахує 7 × 8 і напише результат із підписом» — answer:

```yu
скажи("7 × 8 =", 7 * 8)
```

```text
7 × 8 = 56
```

Each answer sits inside:

````markdown
<details>
<summary>Показати відповідь</summary>

```yu
скажи("Мене звати Оля")
скажи("Я з Києва")
```

</details>
````

Create `docs/book/en/01-start.md` as the same chapter in English: `# Your first program`, the same three sections (`## Say something`, `## More than one line`, `## Yu can count`, `## Try it yourself`), the same programs with English words (`say("Hello, world!")`, `say("My name is Yurii")`, `say("2 + 2 =", 2 + 2)`), and the answers `say("My name is Olya")` / `say("I live in Kyiv")` and `say("7 × 8 =", 7 * 8)`.

- [ ] **Step 4: Run the tests to see them pass**

Run: `cargo test -p yu-core --test book`
Expected: both tests pass. If an output differs, the failure prints the real lines — copy them into the `text` block instead of guessing.

Run the full check: `cargo fmt --all && cargo clippy --all-targets -- -D warnings && cargo test --all`; in `studio/`: `npm run build && npm test`.

- [ ] **Step 5: Commit**

```bash
git add crates/yu-core/tests/book.rs docs/book
git -c user.email=y.dmytrenko14@gmail.com -c user.name="Yurii Dmytrenko" commit -m "The book's test and its first chapter in both languages" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: Numbers and text, conditions, loops

**Files:**
- Create: `docs/book/uk/02-values.md`, `03-conditions.md`, `04-loops.md`
- Create: `docs/book/en/02-values.md`, `03-conditions.md`, `04-loops.md`
- Test: `crates/yu-core/tests/book.rs` (already written in Task 1)

**Interfaces:**
- Consumes: the fenced-block contract of Task 1 (`yu`, `input`, `text`, two `<details>` exercises).
- Produces: chapters 02–04 in both languages; later chapters may use every word introduced here.

- [ ] **Step 1: Write `docs/book/uk/02-values.md`**

`# Числа й текст`, then:

`## Значення мають імена` — a name keeps a value; `=` puts it there:

```yu
вік = 15
скажи("Мені", вік, "років")
```

```text
Мені 15 років
```

`## Рахунок` — `+ - * /` and `%` (the remainder):

```yu
сума = 7 + 8
скажи("Сума:", сума)
скажи("Половина:", сума / 2)
скажи("Остача від ділення на 4:", сума % 4)
```

```text
Сума: 15
Половина: 7.5
Остача від ділення на 4: 3
```

`## Текст склеюється` — `+` joins text, and a number joins to text too:

```yu
ім'я = "Оля"
скажи("Привіт, " + ім'я + "!")
```

```text
Привіт, Оля!
```

`## Запитай у людини` — `запитай` waits for an answer and gives back text:

```input
Оля
```

```yu
ім'я = запитай("Як тебе звати?")
скажи("Привіт, " + ім'я + "!")
```

```text
Привіт, Оля!
```

`## Текст і число — різні речі` — `число` turns text into a number, `текст` goes back:

```input
12
```

```yu
роки = число(запитай("Скільки тобі років?"))
скажи("Через рік буде", роки + 1)
```

```text
Через рік буде 13
```

`## Округли й випадкове` — two more words; the second gives a different number every run, so the book shows no output for it:

```yu
скажи("2.5 округлюється до", округли(2.5))
```

```text
2.5 округлюється до 3
```

```yu
скажи("Кубик:", випадкове(1, 6))
```

`## Спробуй сам`
1. «Запитай два числа й надрукуй їхню суму» — answer (with its own `input` block holding `4` and `5`):

```yu
перше = число(запитай("Перше число:"))
друге = число(запитай("Друге число:"))
скажи("Сума:", перше + друге)
```

```text
Сума: 9
```

2. «Поклади своє місто в ім'я й привітайся з ним» — answer:

```yu
місто = "Київ"
скажи("Привіт, " + місто + "!")
```

```text
Привіт, Київ!
```

- [ ] **Step 2: Write `docs/book/en/02-values.md`**

The same chapter: `# Numbers and text`, sections `## Values have names`, `## Counting`, `## Text joins up`, `## Ask the person`, `## Text and numbers are different`, `## Round and random`, `## Try it yourself`. The same programs with English words; the texts in quotes become: `"I am"` / `"years old"`, `"Sum:"`, `"Half:"`, `"Remainder after dividing by 4:"`, `"Hi, "` with the name `"Olya"`, `"What is your name?"`, `"How old are you?"`, `"Next year you turn"`, `"2.5 rounds to"`, `"Dice:"`, and in the exercises `"First number:"`, `"Second number:"`, `"Sum:"`, the city `"Kyiv"`. Variables become `age`, `sum`, `name`, `years`, `first`, `second`, `city`.

- [ ] **Step 3: Write `docs/book/uk/03-conditions.md`**

`# Умови`, then:

`## Якщо` — a block runs only when the condition is true; the block is indented by four spaces:

```yu
температура = 25
якщо температура > 20:
    скажи("Сьогодні тепло")
```

```text
Сьогодні тепло
```

`## Інакше, інакше якщо` — the first true condition wins:

```yu
бал = 7
якщо бал >= 10:
    скажи("Відмінно")
інакше якщо бал >= 6:
    скажи("Добре")
інакше:
    скажи("Ще трохи")
```

```text
Добре
```

`## І, або, не`:

```yu
вік = 12
якщо вік > 6 і вік < 18:
    скажи("Ходить до школи")
скажи("Дорослий:", не (вік >= 18))
```

```text
Ходить до школи
Дорослий: ні
```

`## Так, ні, нічого`:

```yu
дощ = ні
скажи("Дощ:", дощ)
знахідка = нічого
скажи("Знайшли:", знахідка)
```

```text
Дощ: ні
Знайшли: нічого
```

`## Спробуй сам`
1. «Перевір, чи число парне» — answer:

```yu
число_для_перевірки = 10
якщо число_для_перевірки % 2 == 0:
    скажи("Парне")
інакше:
    скажи("Непарне")
```

```text
Парне
```

2. «Квиток коштує 50, а дітям до 12 — безкоштовно. Надрукуй ціну для віку 10» — answer:

```yu
вік = 10
якщо вік < 12:
    скажи("Ціна: 0")
інакше:
    скажи("Ціна: 50")
```

```text
Ціна: 0
```

- [ ] **Step 4: Write `docs/book/en/03-conditions.md`**

`# Conditions` with sections `## If`, `## Else, else if`, `## And, or, not`, `## True, false, nothing`, `## Try it yourself`. The same programs with English words; the texts become `"It is warm today"`, `"Excellent"` / `"Good"` / `"Almost there"`, `"Goes to school"`, `"Grown up:"`, `"Rain:"`, `"Found:"`, `"Even"` / `"Odd"`, `"Price: 0"` / `"Price: 50"`. Variables become `temperature`, `score`, `age`, `rain`, `found`, `number`.

- [ ] **Step 5: Write `docs/book/uk/04-loops.md`**

`# Цикли`, then:

`## Повтори` — do the same thing N times:

```yu
зірочки = ""
повтори 5 разів:
    зірочки = зірочки + "*"
скажи(зірочки)
```

```text
*****
```

`## Для … від … до` — count, and use the number:

```yu
для i від 1 до 5:
    скажи(i, "× 7 =", i * 7)
```

```text
1 × 7 = 7
2 × 7 = 14
3 × 7 = 21
4 × 7 = 28
5 × 7 = 35
```

`## Крок` — count by more than one:

```yu
для i від 0 до 20 крок 5:
    скажи(i)
```

```text
0
5
10
15
20
```

`## Поки` — repeat while something is true:

```yu
гроші = 10
поки гроші >= 3:
    гроші = гроші - 3
скажи("Лишилось", гроші)
```

```text
Лишилось 1
```

`## Стоп і далі` — leave the loop, or skip to its next round:

```yu
для i від 1 до 10:
    якщо i == 5:
        стоп
    якщо i % 2 == 0:
        далі
    скажи(i)
```

```text
1
3
```

`## Спробуй сам`
1. «Надрукуй зворотний відлік від 5 до 1» — answer:

```yu
для i від 5 до 1 крок -1:
    скажи(i)
```

```text
5
4
3
2
1
```

2. «Порахуй суму чисел від 1 до 100» — answer:

```yu
сума = 0
для i від 1 до 100:
    сума = сума + i
скажи("Сума:", сума)
```

```text
Сума: 5050
```

Note for the countdown: if `крок -1` turns out not to count downwards, the test fails at once —
then write the exercise with `поки`:

```yu
i = 5
поки i >= 1:
    скажи(i)
    i = i - 1
```

- [ ] **Step 6: Write `docs/book/en/04-loops.md`**

`# Loops` with sections `## Repeat`, `## For … from … to`, `## Step`, `## While`, `## Break and continue`, `## Try it yourself`. The same programs with English words; the texts become `"Left"`, `"Sum:"`, and the multiplication line keeps `"× 7 ="`. Variables become `stars`, `money`, `sum`, `i`.

- [ ] **Step 7: Run the tests**

Run: `cargo test -p yu-core --test book`
Expected: both tests pass. Where an output differs, the failure shows the real lines — copy them in.

- [ ] **Step 8: Commit**

```bash
git add docs/book/uk docs/book/en
git -c user.email=y.dmytrenko14@gmail.com -c user.name="Yurii Dmytrenko" commit -m "The book: numbers and text, conditions, loops" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: Lists, functions, drawing

**Files:**
- Create: `docs/book/uk/05-lists.md`, `06-functions.md`, `07-drawing.md`
- Create: `docs/book/en/05-lists.md`, `06-functions.md`, `07-drawing.md`

**Interfaces:**
- Consumes: the block contract; words from chapters 01–04.
- Produces: chapters 05–07. Every example in `07-drawing` must draw — the test checks it.

- [ ] **Step 1: Write `docs/book/uk/05-lists.md`**

`# Списки`, then:

`## Список значень` — square brackets, numbering from 1:

```yu
друзі = ["Оля", "Тарас", "Марко"]
скажи("Перший:", друзі[1])
скажи("Скільки:", довжина(друзі))
```

```text
Перший: Оля
Скільки: 3
```

`## Додай у кінець`:

```yu
покупки = ["хліб"]
додай(покупки, "молоко")
скажи(покупки)
```

```text
["хліб", "молоко"]
```

`## Пройди список` — `для … у`:

```yu
покупки = ["хліб", "молоко", "яблука"]
для що у покупки:
    скажи("Купити:", що)
```

```text
Купити: хліб
Купити: молоко
Купити: яблука
```

`## Порахуй усе разом`:

```yu
оцінки = [7, 9, 10, 8]
сума = 0
для оцінка у оцінки:
    сума = сума + оцінка
скажи("Середня:", сума / довжина(оцінки))
```

```text
Середня: 8.5
```

`## Спробуй сам`
1. «Склади список із трьох улюблених страв і надрукуй кожну» — answer:

```yu
страви = ["борщ", "вареники", "сирники"]
для страва у страви:
    скажи(страва)
```

```text
борщ
вареники
сирники
```

2. «Знайди найбільше число у списку» — answer:

```yu
числа = [3, 17, 8]
найбільше = числа[1]
для n у числа:
    якщо n > найбільше:
        найбільше = n
скажи("Найбільше:", найбільше)
```

```text
Найбільше: 17
```

- [ ] **Step 2: Write `docs/book/en/05-lists.md`**

`# Lists` with sections `## A list of values`, `## Add to the end`, `## Walk the list`, `## Count them all`, `## Try it yourself`. The same programs with English words; the texts become the names `"Olya"`, `"Taras"`, `"Marko"`, `"First:"`, `"How many:"`, `"bread"`, `"milk"`, `"apples"`, `"Buy:"`, `"Average:"`, the dishes `"soup"`, `"pancakes"`, `"dumplings"`, and `"The largest:"`. Variables become `friends`, `shopping`, `item`, `marks`, `mark`, `sum`, `dishes`, `dish`, `numbers`, `largest`, `n`.

- [ ] **Step 3: Write `docs/book/uk/06-functions.md`**

`# Функції`, then:

`## Дай блокові ім'я`:

```yu
функція привітай(ім'я):
    скажи("Привіт, " + ім'я + "!")

привітай("Оля")
привітай("Тарас")
```

```text
Привіт, Оля!
Привіт, Тарас!
```

`## Поверни відповідь`:

```yu
функція площа(ширина, висота):
    поверни ширина * висота

скажи("Площа кімнати:", площа(4, 3))
```

```text
Площа кімнати: 12
```

`## Функція може кликати себе`:

```yu
функція факторіал(n):
    якщо n <= 1:
        поверни 1
    поверни n * факторіал(n - 1)

скажи("5! =", факторіал(5))
```

```text
5! = 120
```

`## Спробуй сам`
1. «Напиши функцію, яка повертає менше з двох чисел» — answer:

```yu
функція менше(a, b):
    якщо a < b:
        поверни a
    поверни b

скажи(менше(9, 4))
```

```text
4
```

2. «Напиши функцію, яка друкує слово N разів» — answer:

```yu
функція повтори_слово(слово, скільки):
    повтори скільки разів:
        скажи(слово)

повтори_слово("Yu", 3)
```

```text
Yu
Yu
Yu
```

- [ ] **Step 4: Write `docs/book/en/06-functions.md`**

`# Functions` with sections `## Give a block a name`, `## Return an answer`, `## A function can call itself`, `## Try it yourself`. The same programs with English words: `greet`, `area`, `factorial`, `smaller`, `repeat_word`; the texts become `"Hi, "`, `"Olya"`, `"Taras"`, `"The room's area:"`, `"5! ="`, and `"Yu"`.

- [ ] **Step 5: Write `docs/book/uk/07-drawing.md`**

`# Малювання`. Every example draws. Explain the canvas first: 600 by 400, x to the right, y down,
the corner 0, 0 is top left.

`## Фон і коло`:

```yu
фон("блакитний")
колір("жовтий")
коло(300, 120, 60)
```

`## Прямокутники`:

```yu
фон("блакитний")
колір("зелений")
прямокутник(0, 300, 600, 100)
колір("білий")
прямокутник(220, 180, 160, 120)
колір("коричневий")
прямокутник(280, 250, 40, 50)
```

`## Лінії й товщина`:

```yu
колір("червоний")
товщина(8)
лінія(100, 300, 300, 150)
лінія(300, 150, 500, 300)
```

`## Напис і своє полотно`:

```yu
полотно(400, 200)
фон("білий")
колір("синій")
напис("Привіт від Yu", 90, 90)
```

A paragraph on saving: the SVG and PNG buttons above the picture in the Studio, and
`yu будинок.yu --svg будинок.svg` in the terminal.

`## Спробуй сам`
1. «Намалюй прапор України» — answer:

```yu
колір("синій")
прямокутник(0, 0, 600, 200)
колір("жовтий")
прямокутник(0, 200, 600, 200)
```

2. «Намалюй обличчя з двох очей і усмішки» — answer:

```yu
фон("жовтий")
колір("чорний")
коло(240, 160, 20)
коло(360, 160, 20)
товщина(10)
лінія(240, 260, 360, 260)
```

- [ ] **Step 6: Write `docs/book/en/07-drawing.md`**

`# Drawing` with sections `## Background and a circle`, `## Rectangles`, `## Lines and thickness`,
`## A label and your own canvas`, `## Try it yourself`. The same programs with English words; the
label becomes `"Hello from Yu"`, and the saving paragraph uses `yu house.yu --svg house.svg`.

- [ ] **Step 7: Run the tests**

Run: `cargo test -p yu-core --test book`
Expected: both tests pass; every example of `07-drawing` draws, or the test names the one that does not.

- [ ] **Step 8: Commit**

```bash
git add docs/book/uk docs/book/en
git -c user.email=y.dmytrenko14@gmail.com -c user.name="Yurii Dmytrenko" commit -m "The book: lists, functions, drawing" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: The turtle, mistakes, three projects

**Files:**
- Create: `docs/book/uk/08-turtle.md`, `09-errors.md`, `10-projects.md`
- Create: `docs/book/en/08-turtle.md`, `09-errors.md`, `10-projects.md`

**Interfaces:**
- Consumes: the block contract, including `yu-error` blocks whose `text` block holds the message exactly as Yu renders it.
- Produces: the last written chapters; after this task both languages hold ten written chapters.

- [ ] **Step 1: Write `docs/book/uk/08-turtle.md`**

`# Черепашка`. Every example draws. The turtle starts in the middle, looking right, with its pen down.

`## Вперед і поворот`:

```yu
колір("синій")
вперед(150)
праворуч(90)
вперед(150)
```

`## Квадрат у циклі`:

```yu
повтори 4 рази:
    вперед(120)
    праворуч(90)
```

`## Заливка`:

```yu
колір("жовтий")
почни_заливку()
повтори 5 разів:
    вперед(200)
    праворуч(144)
заверши_заливку()
```

`## Перо вгору`:

```yu
підніми_перо()
назад(150)
опусти_перо()
колір("зелений")
повтори 3 рази:
    вперед(150)
    ліворуч(120)
```

`## Спіраль`:

```yu
відрізок = 5
повтори 40 разів:
    вперед(відрізок)
    праворуч(20)
    відрізок = відрізок + 3
```

`## Спробуй сам`
1. «Намалюй трикутник функцією» — answer:

```yu
функція трикутник(сторона):
    повтори 3 рази:
        вперед(сторона)
        праворуч(120)

колір("червоний")
трикутник(150)
```

2. «Намалюй квітку з восьми кіл навколо центру» — answer:

```yu
колір("рожевий")
повтори 8 разів:
    коло(300, 120, 40)
    праворуч(45)
```

Note: `коло` draws where you tell it, not where the turtle stands, so if this exercise's picture is
just one circle, change the answer to move the turtle and draw with `вперед`:

```yu
колір("рожевий")
повтори 8 разів:
    вперед(80)
    назад(80)
    праворуч(45)
```

- [ ] **Step 2: Write `docs/book/en/08-turtle.md`**

`# The turtle` with sections `## Forward and turn`, `## A square in a loop`, `## Filling`,
`## Pen up`, `## A spiral`, `## Try it yourself`. The same programs with English words; the
variable becomes `step_length`, the function `triangle(side)`.

- [ ] **Step 3: Write `docs/book/uk/09-errors.md`**

`# Коли щось не так`. Explain that an error is Yu telling you where it stopped and why, and that
the Studio underlines the place in the editor. Each mistake gets a `yu-error` block and the exact
message in a `text` block — run the test and paste what it prints.

`## Невідома назва`:

```yu-error
скаж("Привіт")
```

`## Незакрита лапка`:

```yu-error
скажи("Привіт)
```

`## Забутий відступ`:

```yu-error
якщо 5 > 3:
скажи("Так")
```

`## Ділення на нуль`:

```yu-error
скажи(10 / 0)
```

`## Як читати помилку` — a paragraph about the line number, the caret under the place, and the hint.

`## Спробуй сам`
1. «Знайди помилку й виправ: `скажи("Привіт"` » — answer:

```yu
скажи("Привіт")
```

```text
Привіт
```

2. «Ця програма мала друкувати числа 1, 2, 3, але друкує одне. Виправ» (the broken version is shown
in the exercise as a `yu-error` block only if it really fails; otherwise show it as plain text) —
answer:

```yu
для i від 1 до 3:
    скажи(i)
```

```text
1
2
3
```

- [ ] **Step 4: Write `docs/book/en/09-errors.md`**

`# When something goes wrong` with the same four mistakes (`say` misspelled as `sa`, an unclosed
quote, a missing indent, division by zero), each with the message the test prints when run with
`Lang::En`, and the same two exercises.

- [ ] **Step 5: Write `docs/book/uk/10-projects.md`**

`# Три проєкти`, each project explained in a few sentences and then given whole.

`## Вгадай число`:

```input
5
7
```

```yu
секрет = 7
спроба = число(запитай("Вгадай число від 1 до 10:"))
поки спроба != секрет:
    скажи("Не те. Спробуй ще")
    спроба = число(запитай("Ще раз:"))
скажи("Вгадав!")
```

```text
Не те. Спробуй ще
Вгадав!
```

`## Будинок` — a drawing that uses the turtle for the roof:

```yu
фон("блакитний")
колір("зелений")
прямокутник(0, 300, 600, 100)
колір("білий")
прямокутник(220, 180, 160, 120)
колір("червоний")
почни_заливку()
повтори 3 рази:
    вперед(160)
    ліворуч(120)
заверши_заливку()
```

`## Вікторина`:

```input
Київ
4
```

```yu
бал = 0
якщо запитай("Столиця України?") == "Київ":
    бал = бал + 1
якщо число(запитай("2 + 2 = ?")) == 4:
    бал = бал + 1
скажи("Твій бал:", бал, "з 2")
```

```text
Твій бал: 2 з 2
```

`## Спробуй сам`
1. «Додай у вікторину третє питання» — answer: the quiz above with one more `якщо` and `з 3`,
   with its own `input` block.
2. «Порахуй, скільки спроб пішло на вгадування» — answer: the guessing game with a counter.

- [ ] **Step 6: Write `docs/book/en/10-projects.md`**

`# Three projects` with `## Guess the number`, `## A house`, `## A quiz`, `## Try it yourself`.
The same programs with English words; the questions become `"Guess a number from 1 to 10:"`,
`"Not that one. Try again"`, `"Once more:"`, `"You got it!"`, `"The capital of Ukraine?"` with the
answer `"Kyiv"`, `"2 + 2 = ?"`, `"Your score:"`, `"out of 2"`.

- [ ] **Step 7: Run the tests**

Run: `cargo test -p yu-core --test book`
Expected: both tests pass. The error chapter is where the messages must match exactly — paste what
the test prints.

- [ ] **Step 8: Commit**

```bash
git add docs/book/uk docs/book/en
git -c user.email=y.dmytrenko14@gmail.com -c user.name="Yurii Dmytrenko" commit -m "The book: the turtle, mistakes and three projects" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: The pages: generator, reference chapter, look and build

**Files:**
- Create: `studio/src/book-tools.ts`, `studio/scripts/build-book.mjs`, `studio/src/book.css`, `studio/src/book.ts`
- Modify: `studio/vite.config.ts`, `studio/package.json`, `studio/.gitignore`
- Create (generated, committed): `docs/book/uk/11-words.md`, `docs/book/en/11-words.md`
- Modify: `crates/yu-core/tests/book.rs` (the book now has eleven chapters)
- Test: `studio/tests/book.test.ts`

**Interfaces:**
- Consumes: `loadYu` and `Yu.library()`/`Yu.names()` from `src/bridge.ts`; `encode` from `src/share.ts`; `yuParser` from `src/yu-lang.ts`; the theme tokens of `src/theme.css`.
- Produces:
  - `src/book-tools.ts`: `slug(text): string`, `title(markdown): string`, `headings(markdown): { text: string; anchor: string }[]`, `chapterList(files: { file: string; markdown: string }[], lang: string): Chapter[]`, `highlightToHtml(code, parser): string`, `escapeHtml(text): string`, and `interface Chapter { id, file, title, url, headings }`.
  - `npm run book` writes `studio/book/<lang>/<file>/index.html`, `studio/book/index.html`, `studio/public/book/search-<lang>.json`, `studio/public/sitemap.xml`, `studio/public/robots.txt`, and both `11-words.md`.
  - Page ids the browser tests use: `#book-search`, `#book-results`, `#book-theme`, `.chapters`, `.chapter`, `.example .open`, `.book-foot`.

- [ ] **Step 1: Write the failing unit tests**

Create `studio/tests/book.test.ts`:

````ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { chapterList, escapeHtml, headings, highlightToHtml, slug, title } from '../src/book-tools.ts';
import { yuParser } from '../src/yu-lang.ts';

const chapter = [
  '# Цикли',
  '',
  'Текст розділу.',
  '',
  '## Повтори',
  '',
  '```yu',
  '# Це коментар, а не заголовок',
  'повтори 2 рази:',
  '    скажи("Крок")',
  '```',
  '',
  '## Спробуй сам',
  '',
].join('\n');

test('a heading becomes a short anchor', () => {
  assert.equal(slug('Спробуй сам'), 'спробуй-сам');
  assert.equal(slug('For … from … to'), 'for-from-to');
  assert.equal(slug('Так, ні, нічого'), 'так-ні-нічого');
});

test('the title and the headings skip what is inside code', () => {
  assert.equal(title(chapter), 'Цикли');
  assert.deepEqual(headings(chapter), [
    { text: 'Повтори', anchor: 'повтори' },
    { text: 'Спробуй сам', anchor: 'спробуй-сам' },
  ]);
});

test('chapters keep their file order and know their address', () => {
  const list = chapterList(
    [
      { file: '04-loops', markdown: chapter },
      { file: '01-start', markdown: '# Перша програма\n' },
    ],
    'uk',
  );
  assert.deepEqual(
    list.map((c) => [c.file, c.id, c.title, c.url]),
    [
      ['01-start', 'start', 'Перша програма', '/book/uk/01-start/'],
      ['04-loops', 'loops', 'Цикли', '/book/uk/04-loops/'],
    ],
  );
});

test('code turns into coloured, escaped HTML', () => {
  const parser = yuParser(new Set(['повтори', 'разів']), new Set(['скажи']));
  const html = highlightToHtml('скажи("<&>")', parser);
  assert.match(html, /<span class="tok-variableName-standard">скажи<\/span>/);
  assert.match(html, /&lt;&amp;&gt;/);
  assert.equal(escapeHtml('a<b>"c"&d'), 'a&lt;b&gt;&quot;c&quot;&amp;d');
});
````

- [ ] **Step 2: Run the tests to see them fail**

Run (in `studio/`): `npm test`
Expected: FAIL — `Cannot find module '…/src/book-tools.ts'`.

- [ ] **Step 3: Write the helpers**

Create `studio/src/book-tools.ts`:

```ts
// Pieces the book's generator and its pages share: anchors, chapter lists and coloured code.
import { StringStream, type StreamParser } from '@codemirror/language';

export interface Chapter {
  /** The file name without its number, e.g. `loops`. */
  id: string;
  /** The file name, e.g. `04-loops`. */
  file: string;
  title: string;
  /** Where the page lives, e.g. `/book/uk/04-loops/`. */
  url: string;
  headings: { text: string; anchor: string }[];
}

export function escapeHtml(text: string): string {
  const named: Record<string, string> = { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' };
  return text.replace(/[&<>"]/g, (character) => named[character] ?? character);
}

/** A heading's anchor: lower case, letters and digits kept, everything else a dash. */
export function slug(text: string): string {
  return text
    .toLowerCase()
    .replace(/[^\p{L}\p{N}]+/gu, '-')
    .replace(/^-+|-+$/g, '');
}

/** The lines of `markdown` that are not inside a fenced block. */
function prose(markdown: string): string[] {
  const out: string[] = [];
  let inside = false;
  for (const line of markdown.split('\n')) {
    if (line.startsWith('```')) {
      inside = !inside;
      continue;
    }
    if (!inside) out.push(line);
  }
  return out;
}

/** The chapter's `# ` heading; a `#` comment inside an example is not one. */
export function title(markdown: string): string {
  const line = prose(markdown).find((row) => row.startsWith('# '));
  return line ? line.slice(2).trim() : '';
}

/** Every `## ` heading of the chapter, with its anchor. */
export function headings(markdown: string): { text: string; anchor: string }[] {
  return prose(markdown)
    .filter((line) => line.startsWith('## '))
    .map((line) => {
      const text = line.slice(3).trim();
      return { text, anchor: slug(text) };
    });
}

/** The chapters of one language, in file order. */
export function chapterList(
  files: { file: string; markdown: string }[],
  lang: string,
): Chapter[] {
  return files
    .slice()
    .sort((a, b) => a.file.localeCompare(b.file))
    .map(({ file, markdown }) => ({
      id: file.replace(/^\d+-/, ''),
      file,
      title: title(markdown),
      url: `/book/${lang}/${file}/`,
      headings: headings(markdown),
    }));
}

/** Code coloured with the editor's own tokenizer, as HTML. */
export function highlightToHtml(
  code: string,
  parser: StreamParser<Record<string, never>>,
): string {
  let out = '';
  code.split('\n').forEach((line, index) => {
    if (index > 0) out += '\n';
    const stream = new StringStream(line, 4, 4);
    while (!stream.eol()) {
      const style = parser.token(stream, {});
      const piece = escapeHtml(stream.current());
      out += style ? `<span class="tok-${style.replace('.', '-')}">${piece}</span>` : piece;
      stream.start = stream.pos;
    }
  });
  return out;
}
```

- [ ] **Step 4: Run the unit tests to see them pass**

Run (in `studio/`): `npm test`
Expected: every test passes, the four new ones included.

- [ ] **Step 5: Add the generator**

Install the converter: `npm install --save-exact --save-dev markdown-it@15.0.2`

Create `studio/scripts/build-book.mjs`:

````js
// Builds the book: the Markdown in ../docs/book becomes pages in book/, the reference chapter
// comes from the core's library, and the search index, sitemap and robots.txt come along.
import { mkdirSync, readFileSync, readdirSync, rmSync, writeFileSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';
import MarkdownIt from 'markdown-it';
import { loadYu } from '../src/bridge.ts';
import { encode } from '../src/share.ts';
import { yuParser } from '../src/yu-lang.ts';
import { chapterList, escapeHtml, highlightToHtml, slug } from '../src/book-tools.ts';

const studio = dirname(dirname(fileURLToPath(import.meta.url)));
const root = dirname(studio);
const source = join(root, 'docs', 'book');
const pages = join(studio, 'book');
const SITE = 'https://yu-lang.vercel.app';
const LANGS = ['uk', 'en'];

const WORDS = {
  uk: {
    book: 'Книга Yu',
    studio: 'Студія',
    search: 'Шукати в книзі',
    searchExample: 'Наприклад, цикл',
    open: 'Відкрити в Студії',
    input: 'Ти вводиш',
    output: 'Вивід',
    previous: 'Попередній розділ',
    next: 'Наступний розділ',
    chapters: 'Розділи',
    theme: 'Змінити тему',
    other: 'English',
    skip: 'До тексту',
    nothing: 'Нічого не знайшлося',
    reference: 'Усі слова',
    referenceIntro:
      'Кожне слово мови Yu: як його писати, що воно робить і приклад. Цей розділ збирається з ядра мови, тому він завжди свіжий.',
    colours: 'Кольори',
    coloursIntro:
      'Назву кольору пиши в лапках. Підходить будь-який рід («червона», «червоне»), англійська назва, «#ff8800» або «випадковий».',
    colourName: 'Українською',
    colourEn: 'English',
    colourHex: 'Код',
  },
  en: {
    book: 'The Yu book',
    studio: 'The Studio',
    search: 'Search the book',
    searchExample: 'For example, loops',
    open: 'Open in Studio',
    input: 'You type',
    output: 'Output',
    previous: 'Previous chapter',
    next: 'Next chapter',
    chapters: 'Chapters',
    theme: 'Switch theme',
    other: 'Українською',
    skip: 'To the text',
    nothing: 'Nothing found',
    reference: 'All words',
    referenceIntro:
      'Every word of Yu: how to write it, what it does and an example. This chapter is built from the language core, so it is never out of date.',
    colours: 'Colours',
    coloursIntro:
      'Write a colour name in quotes. Ukrainian names in any gender form, English names, "#ff8800" and "random" all work.',
    colourName: 'Ukrainian',
    colourEn: 'English',
    colourHex: 'Code',
  },
};

const yu = await loadYu(readFileSync(join(studio, 'public', 'yu_wasm.wasm')), {
  print: () => {},
  ask: () => null,
});
const names = yu.names();
const library = yu.library();
const parser = yuParser(
  new Set(names.keywords),
  new Set(names.builtins.flatMap((builtin) => [builtin.uk, builtin.en])),
);

/** The reference chapter, written from the core's library. */
function reference(lang) {
  const words = WORDS[lang];
  const pick = (uk, en) => (lang === 'uk' ? uk : en);
  let out = `<!-- Generated by studio/scripts/build-book.mjs from crates/yu-core/src/library.rs -->\n\n`;
  out += `# ${words.reference}\n\n${words.referenceIntro}\n\n`;
  for (const section of library.sections) {
    out += `## ${pick(section.uk, section.en)}\n\n`;
    for (const entry of section.entries) {
      out += `### ${pick(entry.uk, entry.en)} · ${pick(entry.en, entry.uk)}\n\n`;
      out += `\`${pick(entry.form_uk, entry.form_en)}\` — ${pick(entry.text_uk, entry.text_en)}\n\n`;
      out += '```yu\n' + pick(entry.ex_uk, entry.ex_en) + '\n```\n\n';
    }
  }
  out += `## ${words.colours}\n\n${words.coloursIntro}\n\n`;
  out += `| ${words.colourName} | ${words.colourEn} | ${words.colourHex} |\n|---|---|---|\n`;
  for (const colour of library.colors) {
    out += `| ${colour.uk} | ${colour.en} | \`${colour.hex}\` |\n`;
  }
  return out + '\n';
}

const md = new MarkdownIt({ html: true });

md.renderer.rules.fence = (tokens, index, options, env) => {
  const token = tokens[index];
  const info = token.info.trim();
  const code = token.content.replace(/\n$/, '');
  const words = WORDS[env.lang];
  if (info === 'yu' || info === 'yu-error') {
    const broken = info === 'yu-error' ? ' broken' : '';
    return (
      `<figure class="example${broken}">` +
      `<pre class="snippet">${highlightToHtml(code, parser)}</pre>` +
      `<a class="ghost small open" href="${env.links.get(code)}">${words.open}</a>` +
      `</figure>`
    );
  }
  const label = info === 'input' ? words.input : info === 'text' ? words.output : '';
  const body = `<pre class="out">${escapeHtml(code)}</pre>`;
  return label
    ? `<figure class="result"><figcaption>${label}</figcaption>${body}</figure>`
    : body;
};

md.renderer.rules.heading_open = (tokens, index) => {
  const tag = tokens[index].tag;
  if (tag !== 'h2' && tag !== 'h3') return `<${tag}>`;
  return `<${tag} id="${slug(tokens[index + 1].content)}">`;
};

/** A chapter as HTML; every example gets its share link first, because encoding waits. */
async function chapterHtml(markdown, lang) {
  const tokens = md.parse(markdown, {});
  const links = new Map();
  for (const token of tokens) {
    const info = token.info?.trim();
    if (token.type !== 'fence' || (info !== 'yu' && info !== 'yu-error')) continue;
    const code = token.content.replace(/\n$/, '');
    if (!links.has(code)) links.set(code, `/#code=${await encode(code)}`);
  }
  return md.renderer.render(tokens, md.options, { lang, links });
}

/** The first sentence of the chapter, for the page's description. */
function summary(markdown) {
  const line = markdown
    .split('\n')
    .find((row) => row.trim() && !row.startsWith('#') && !row.startsWith('<') && !row.startsWith('```'));
  return escapeHtml((line ?? '').trim().slice(0, 160));
}

function page({ lang, chapter, chapters, html }) {
  const words = WORDS[lang];
  const other = lang === 'uk' ? 'en' : 'uk';
  const at = chapters.findIndex((one) => one.file === chapter.file);
  const previous = chapters[at - 1];
  const next = chapters[at + 1];
  const list = chapters
    .map((one) => {
      const current = one.file === chapter.file ? ' aria-current="page"' : '';
      return `<li><a href="${one.url}"${current}>${escapeHtml(one.title)}</a></li>`;
    })
    .join('');
  const steps = [
    previous ? `<a class="ghost" href="${previous.url}">← ${escapeHtml(previous.title)}</a>` : '<span></span>',
    next ? `<a class="ghost" href="${next.url}">${escapeHtml(next.title)} →</a>` : '<span></span>',
  ].join('');
  const ld = JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'Article',
    headline: chapter.title,
    inLanguage: lang,
    isPartOf: { '@type': 'Book', name: words.book, url: `${SITE}/book/${lang}/` },
    author: { '@type': 'Person', name: 'Yurii Dmytrenko' },
  });
  return `<!doctype html>
<html lang="${lang}" data-theme="dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>${escapeHtml(chapter.title)} — ${words.book}</title>
    <meta name="description" content="${summary(chapter.markdown)}" />
    <meta name="color-scheme" content="dark light" />
    <link rel="icon" href="/logo.svg" type="image/svg+xml" />
    <link rel="canonical" href="${SITE}${chapter.url}" />
    <link rel="alternate" hreflang="uk" href="${SITE}/book/uk/${chapter.file}/" />
    <link rel="alternate" hreflang="en" href="${SITE}/book/en/${chapter.file}/" />
    <script>try{var t=localStorage.getItem('yu-theme');if(t)document.documentElement.dataset.theme=t}catch(e){}</script>
    <script type="application/ld+json">${ld}</script>
  </head>
  <body class="book" data-lang="${lang}">
    <a class="skip" href="#text">${words.skip}</a>
    <header class="menubar">
      <a class="logo" href="/"><img src="/logo.svg" alt="Yu" width="22" height="22" /></a>
      <a class="book-name" href="/book/${lang}/01-start/">${words.book}</a>
      <div class="finder">
        <input id="book-search" class="search" type="search" placeholder="${words.searchExample}" aria-label="${words.search}" autocomplete="off" />
        <div id="book-results" class="results" hidden></div>
      </div>
      <span class="spacer"></span>
      <a class="ghost" href="/">${words.studio}</a>
      <a class="ghost" id="book-other" href="/book/${other}/${chapter.file}/" data-lang="${other}">${words.other}</a>
      <button id="book-theme" class="ghost icon" type="button" title="${words.theme}" aria-label="${words.theme}">
        <svg viewBox="0 0 24 24" width="14" height="14" aria-hidden="true"><circle cx="12" cy="12" r="8" fill="none" stroke="currentColor" stroke-width="2" /><path d="M12 4a8 8 0 0 1 0 16z" fill="currentColor" /></svg>
      </button>
    </header>
    <main class="book-body">
      <nav class="chapters" aria-label="${words.chapters}">
        <details open>
          <summary>${words.chapters}</summary>
          <ol>${list}</ol>
        </details>
      </nav>
      <article id="text" class="chapter">${html}</article>
    </main>
    <footer class="book-foot">${steps}</footer>
    <script type="module" src="/src/book.ts"></script>
  </body>
</html>
`;
}

/** The page at /book/: both languages, and a jump to the reader's own. */
function picker(chapters) {
  const link = (lang) =>
    `<a class="primary" href="${chapters[lang][0].url}">${WORDS[lang].book} · ${lang.toUpperCase()}</a>`;
  return `<!doctype html>
<html lang="uk" data-theme="dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>${WORDS.uk.book} · ${WORDS.en.book}</title>
    <meta name="color-scheme" content="dark light" />
    <link rel="icon" href="/logo.svg" type="image/svg+xml" />
    <link rel="canonical" href="${SITE}/book/" />
    <script>try{var t=localStorage.getItem('yu-theme');if(t)document.documentElement.dataset.theme=t;var l=localStorage.getItem('yu-lang')||(navigator.language||'').slice(0,2);location.replace(l==='en'?'/book/en/01-start/':'/book/uk/01-start/')}catch(e){}</script>
  </head>
  <body class="book picker">
    <main class="pick">${link('uk')}${link('en')}</main>
    <script type="module" src="/src/book.ts"></script>
  </body>
</html>
`;
}

rmSync(pages, { recursive: true, force: true });
const chapters = {};
for (const lang of LANGS) {
  const dir = join(source, lang);
  writeFileSync(join(dir, '11-words.md'), reference(lang));
  const files = readdirSync(dir)
    .filter((name) => name.endsWith('.md'))
    .map((name) => ({ file: name.replace(/\.md$/, ''), markdown: readFileSync(join(dir, name), 'utf8') }));
  chapters[lang] = chapterList(files, lang).map((chapter) => ({
    ...chapter,
    markdown: files.find((one) => one.file === chapter.file).markdown,
  }));
}

for (const lang of LANGS) {
  for (const chapter of chapters[lang]) {
    const html = await chapterHtml(chapter.markdown, lang);
    const dir = join(pages, lang, chapter.file);
    mkdirSync(dir, { recursive: true });
    writeFileSync(join(dir, 'index.html'), page({ lang, chapter, chapters: chapters[lang], html }));
  }
  const index = chapters[lang].map(({ title, url, headings }) => ({ title, url, headings }));
  mkdirSync(join(studio, 'public', 'book'), { recursive: true });
  writeFileSync(join(studio, 'public', 'book', `search-${lang}.json`), JSON.stringify(index));
}
mkdirSync(pages, { recursive: true });
writeFileSync(join(pages, 'index.html'), picker(chapters));

const urls = ['/', '/book/', ...LANGS.flatMap((lang) => chapters[lang].map((chapter) => chapter.url))];
writeFileSync(
  join(studio, 'public', 'sitemap.xml'),
  `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n` +
    urls.map((url) => `  <url><loc>${SITE}${url}</loc></url>`).join('\n') +
    `\n</urlset>\n`,
);
writeFileSync(
  join(studio, 'public', 'robots.txt'),
  `User-agent: *\nAllow: /\nSitemap: ${SITE}/sitemap.xml\n`,
);

console.log(`book: ${LANGS.map((lang) => `${lang} ${chapters[lang].length}`).join(', ')} chapters`);
````

- [ ] **Step 6: Teach the build about the book**

In `studio/package.json`, add to `scripts`: `"book": "node scripts/build-book.mjs"`, and change
`"dev"` to `"npm run wasm && npm run book && vite"` and `"build"` to
`"npm run wasm && npm run book && tsc --noEmit && vite build"`.

In `studio/.gitignore`, add:

```
book/
public/book/
public/sitemap.xml
public/robots.txt
```

In `studio/vite.config.ts`, add the generated pages as build inputs:

```ts
import { readdirSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { defineConfig } from 'vite';

// SharedArrayBuffer (how запитай waits for an answer) needs a cross-origin isolated page.
const isolation = {
  'Cross-Origin-Opener-Policy': 'same-origin',
  'Cross-Origin-Embedder-Policy': 'require-corp',
};

/** Every page `npm run book` wrote, so Vite builds them beside the Studio. */
function bookPages(): Record<string, string> {
  const dir = fileURLToPath(new URL('./book', import.meta.url));
  let names: string[] = [];
  try {
    names = readdirSync(dir, { recursive: true })
      .map((name) => String(name).replace(/\\/g, '/'))
      .filter((name) => name.endsWith('index.html'));
  } catch {
    return {}; // the book has not been generated yet
  }
  return Object.fromEntries(
    names.map((name) => [`book-${name.replace(/\/?index\.html$/, '').replace(/\//g, '-') || 'home'}`, `${dir.replace(/\\/g, '/')}/${name}`]),
  );
}

export default defineConfig({
  server: { headers: isolation, fs: { allow: ['..'] } },
  preview: { headers: isolation },
  worker: { format: 'es' },
  build: {
    target: 'es2022',
    rollupOptions: {
      input: {
        main: fileURLToPath(new URL('./index.html', import.meta.url)),
        ...bookPages(),
      },
    },
  },
});
```

- [ ] **Step 7: Write the page's script and its look**

Create `studio/src/book.ts`:

```ts
// The book's pages: the theme they share with the Studio, the language they remember, the search.
import '@fontsource-variable/inter';
import '@fontsource-variable/jetbrains-mono';
import './theme.css';
import './book.css';

interface Found {
  title: string;
  url: string;
  headings: { text: string; anchor: string }[];
}

/** localStorage may be missing or throw (private windows); the page works without it. */
const store = {
  get(key: string): string | null {
    try {
      return localStorage.getItem(key);
    } catch {
      return null;
    }
  },
  set(key: string, value: string): void {
    try {
      localStorage.setItem(key, value);
    } catch {
      // Not remembered this time.
    }
  },
};

const lang = document.body.dataset.lang === 'en' ? 'en' : 'uk';

document.getElementById('book-theme')?.addEventListener('click', () => {
  const next = document.documentElement.dataset.theme === 'light' ? 'dark' : 'light';
  document.documentElement.dataset.theme = next;
  store.set('yu-theme', next);
});

// Reading the book in one language means the Studio opens in it too.
document.getElementById('book-other')?.addEventListener('click', (event) => {
  const other = (event.currentTarget as HTMLElement).dataset.lang;
  if (other) store.set('yu-lang', other);
});

const search = document.getElementById('book-search') as HTMLInputElement | null;
const results = document.getElementById('book-results');

if (search && results) {
  let chapters: Found[] = [];
  const load = fetch(`/book/search-${lang}.json`)
    .then((response) => response.json() as Promise<Found[]>)
    .then((found) => {
      chapters = found;
    })
    .catch(() => {
      chapters = [];
    });

  const show = () => {
    const query = search.value.trim().toLowerCase();
    if (!query) {
      results.hidden = true;
      results.replaceChildren();
      return;
    }
    const hits: { text: string; url: string }[] = [];
    for (const chapter of chapters) {
      if (chapter.title.toLowerCase().includes(query)) hits.push({ text: chapter.title, url: chapter.url });
      for (const heading of chapter.headings) {
        if (heading.text.toLowerCase().includes(query)) {
          hits.push({ text: `${chapter.title} · ${heading.text}`, url: `${chapter.url}#${heading.anchor}` });
        }
      }
    }
    results.replaceChildren(
      ...hits.slice(0, 8).map((hit) => {
        const link = document.createElement('a');
        link.href = hit.url;
        link.textContent = hit.text;
        return link;
      }),
    );
    if (!hits.length) {
      const empty = document.createElement('p');
      empty.className = 'none';
      empty.textContent = lang === 'uk' ? 'Нічого не знайшлося' : 'Nothing found';
      results.append(empty);
    }
    results.hidden = false;
  };

  search.addEventListener('input', () => void load.then(show));
  search.addEventListener('keydown', (event) => {
    if (event.key !== 'Enter') return;
    const first = results.querySelector('a');
    if (first) location.href = first.href;
  });
  document.addEventListener('click', (event) => {
    if (!results.contains(event.target as Node) && event.target !== search) results.hidden = true;
  });
}
```

Create `studio/src/book.css`:

```css
/* The book's pages, on the Studio's tokens. */
.book {
  display: block;
  height: auto;
  background: var(--page);
}

.skip {
  position: absolute;
  left: -9999px;
  top: 0;
  z-index: 50;
  border-radius: 6px;
  background: var(--accent);
  color: var(--on-accent);
  padding: 8px 14px;
}

.skip:focus {
  left: 12px;
  top: 12px;
}

.book .menubar {
  position: sticky;
  top: 0;
  z-index: 20;
  background: var(--page);
}

.book-name {
  color: var(--text);
  font-size: 15px;
  font-weight: 600;
  text-decoration: none;
}

.book .ghost {
  display: inline-flex;
  align-items: center;
  color: var(--text);
  text-decoration: none;
}

.finder {
  position: relative;
  flex: 0 1 260px;
  margin-left: 8px;
}

.finder .search {
  width: 100%;
  margin: 0;
}

.results {
  position: absolute;
  z-index: 30;
  top: 38px;
  left: 0;
  width: min(420px, 80vw);
  display: flex;
  flex-direction: column;
  padding: 5px;
  border: 1px solid var(--border);
  border-radius: 8px;
  background: var(--raised);
  box-shadow: var(--popup-shadow);
}

.results a {
  border-radius: 5px;
  padding: 7px 10px;
  color: var(--text);
  font-size: 13px;
  text-decoration: none;
}

.results a:hover {
  background: var(--accent-soft);
}

.results .none {
  margin: 0;
  padding: 7px 10px;
  color: var(--muted);
  font-size: 13px;
}

.book-body {
  display: grid;
  grid-template-columns: 240px minmax(0, 1fr);
  gap: 32px;
  max-width: 1120px;
  margin: 0 auto;
  padding: 24px 20px 48px;
}

.chapters {
  align-self: start;
  position: sticky;
  top: 60px;
}

.chapters summary {
  display: none;
}

.chapters ol {
  margin: 0;
  padding: 0;
  list-style: none;
  counter-reset: chapter;
}

.chapters a {
  display: block;
  border-radius: 6px;
  padding: 6px 10px;
  color: var(--muted);
  font-size: 13.5px;
  text-decoration: none;
}

.chapters a::before {
  counter-increment: chapter;
  content: counter(chapter) '. ';
  color: var(--muted);
}

.chapters a:hover {
  background: var(--accent-soft);
  color: var(--text);
}

.chapters a[aria-current='page'] {
  background: var(--accent-soft);
  box-shadow: inset 2px 0 var(--accent-text);
  color: var(--text);
}

.chapter {
  max-width: 70ch;
  font-size: 16px;
  line-height: 1.75;
}

.chapter h1 {
  margin: 0 0 20px;
  font-size: 30px;
  line-height: 1.2;
}

.chapter h2 {
  margin: 36px 0 12px;
  font-size: 21px;
}

.chapter h3 {
  margin: 28px 0 10px;
  font-size: 17px;
}

.chapter p {
  margin: 0 0 16px;
}

.chapter ul,
.chapter ol {
  margin: 0 0 16px;
  padding-left: 24px;
}

.chapter li {
  margin-bottom: 8px;
}

.chapter code {
  border-radius: 4px;
  background: var(--accent-soft);
  padding: 1px 5px;
  font: 14px var(--font-code);
}

.chapter table {
  width: 100%;
  margin-bottom: 20px;
  border-collapse: collapse;
  font-size: 14px;
}

.chapter th,
.chapter td {
  border-bottom: 1px solid var(--border);
  padding: 7px 10px;
  text-align: left;
}

.example {
  margin: 0 0 20px;
  border: 1px solid var(--border);
  border-radius: 10px;
  background: var(--surface);
  padding: 12px 14px;
}

.example.broken {
  border-color: color-mix(in srgb, var(--error) 45%, var(--border));
}

.example .snippet {
  margin: 0 0 10px;
  overflow-x: auto;
  background: none;
  padding: 0;
  font: 13.5px/1.7 var(--font-code);
  white-space: pre;
}

.example .open {
  height: 26px;
  padding: 0 10px;
  font-size: 12px;
}

.result {
  margin: -8px 0 20px;
}

.result figcaption {
  margin-bottom: 4px;
  color: var(--muted);
  font-size: 12px;
  letter-spacing: 0.04em;
  text-transform: uppercase;
}

.result .out,
.chapter .out {
  margin: 0;
  overflow-x: auto;
  border-radius: 8px;
  background: var(--surface);
  padding: 10px 14px;
  font: 13.5px/1.65 var(--font-code);
  white-space: pre-wrap;
}

.chapter details {
  margin: 0 0 16px;
  border: 1px solid var(--border);
  border-radius: 10px;
  background: var(--raised);
  padding: 10px 14px;
}

.chapter details summary {
  cursor: pointer;
  font-weight: 600;
}

.chapter details[open] summary {
  margin-bottom: 12px;
}

.book-foot {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  max-width: 1120px;
  margin: 0 auto;
  padding: 0 20px 48px;
}

.picker .pick {
  display: grid;
  gap: 16px;
  place-content: center;
  min-height: 100dvh;
  padding: 24px;
}

.picker .primary {
  padding: 14px 24px;
  text-align: center;
  text-decoration: none;
}

@media (max-width: 900px) {
  .book-body {
    grid-template-columns: minmax(0, 1fr);
    gap: 16px;
  }

  .chapters {
    position: static;
  }

  .chapters summary {
    display: list-item;
    cursor: pointer;
    padding: 6px 0;
    font-weight: 600;
  }

  .finder {
    flex: 1 1 140px;
  }
}
```

- [ ] **Step 8: Build the book and look at it**

Run (in `studio/`): `npm run book`
Expected: `book: uk 11, en 11 chapters`, and `docs/book/uk/11-words.md` now exists.

Run (in `studio/`): `npm run build && npm test`
Expected: the build writes `dist/book/uk/01-start/index.html` and the rest; every unit test passes.

- [ ] **Step 9: The book now has eleven chapters**

In `crates/yu-core/tests/book.rs`, inside `the_chapters_are_shaped_alike`, after the line that
compares `uk` with `en`, add:

```rust
    assert_eq!(uk.len(), 11, "the book has ten written chapters and the reference: {uk:?}");
```

Run: `cargo test -p yu-core --test book`
Expected: both tests pass, the reference chapter's examples included.

- [ ] **Step 10: Commit**

```bash
git add studio/src/book-tools.ts studio/src/book.ts studio/src/book.css studio/scripts/build-book.mjs studio/vite.config.ts studio/package.json studio/package-lock.json studio/.gitignore studio/tests/book.test.ts docs/book/uk/11-words.md docs/book/en/11-words.md crates/yu-core/tests/book.rs
git -c user.email=y.dmytrenko14@gmail.com -c user.name="Yurii Dmytrenko" commit -m "The book's pages: generator, reference from the core, look and search" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: The Studio points at the book

**Files:**
- Modify: `studio/src/i18n.ts` (a word in both languages), `studio/src/main.ts` (a command and the Help menu)

**Interfaces:**
- Consumes: the command table and `MenuDef` of the Studio; the pages of Task 5.
- Produces: the command id `book`, which the browser test looks for as `.menu [data-command="book"]`.

- [ ] **Step 1: Add the word**

In `studio/src/i18n.ts`, add to `UK` right after `libraryWords`:

```ts
  book: 'Книга Yu',
```

and to `EN` in the same place:

```ts
  book: 'The Yu book',
```

- [ ] **Step 2: Add the command and put it in Help**

In `studio/src/main.ts`, add to the `commands` array right after the `library-words` entry:

```ts
  {
    id: 'book',
    label: () => 'book',
    run: () => void window.open(`/book/${lang}/01-start/`, '_blank', 'noopener'),
  },
```

and change the Help menu to:

```ts
  { title: 'menuHelp', items: ['library-words', 'book', 'github'] },
```

- [ ] **Step 3: Check it**

Run (in `studio/`): `npm run build && npm test`
Expected: the build succeeds and every unit test passes.

Start the preview (`npm run preview -- --port 4180 --strictPort`), open the Studio, and use
Довідка → Книга Yu: the book opens at the first chapter in the interface language.

- [ ] **Step 4: Commit**

```bash
git add studio/src/i18n.ts studio/src/main.ts
git -c user.email=y.dmytrenko14@gmail.com -c user.name="Yurii Dmytrenko" commit -m "The Studio's Help menu opens the book" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: Browser tests for the book

**Files:**
- Create: `studio/tests/book.spec.ts`

**Interfaces:**
- Consumes: the page ids of Task 5 (`#book-search`, `#book-results`, `#book-other`, `.chapters`, `.example .open`), the Studio's `#st-theme`, and the command id `book` of Task 6.

- [ ] **Step 1: Write the tests**

Create `studio/tests/book.spec.ts`:

```ts
import { expect, test } from '@playwright/test';

let errors: string[] = [];

test.beforeEach(({ page }) => {
  errors = [];
  page.on('pageerror', (error) => errors.push(error.message));
});

test.afterEach(() => {
  expect(errors).toEqual([]);
});

test('a chapter shows its heading and the whole book beside it', async ({ page }) => {
  await page.goto('/book/uk/01-start/');
  await expect(page.locator('h1')).toHaveText('Перша програма');
  await expect(page.locator('.chapters a')).toHaveCount(11);
  await expect(page.locator('.chapters a[aria-current="page"]')).toHaveText(/Перша програма/);
  await expect(page.locator('.book-foot a')).toHaveCount(1);
});

test('an example opens in the Studio with its code', async ({ page }) => {
  await page.goto('/book/uk/01-start/');
  const open = page.locator('.example .open').first();
  await expect(open).toHaveAttribute('href', /^\/#code=/);
  await open.click();
  await expect(page.locator('.cm-content')).toContainText('скажи("Привіт, світе!")');
});

test('the theme picked in the Studio is the one the book opens with', async ({ page }) => {
  await page.goto('/');
  await page.locator('#st-theme').click();
  const theme = await page.evaluate(() => document.documentElement.dataset.theme);
  await page.goto('/book/uk/04-loops/');
  await expect(page.locator('html')).toHaveAttribute('data-theme', String(theme));
});

test('search finds a chapter and Enter opens it', async ({ page }) => {
  await page.goto('/book/uk/01-start/');
  await page.locator('#book-search').fill('цикл');
  await expect(page.locator('#book-results a').first()).toContainText('Цикли');
  await page.locator('#book-search').press('Enter');
  await expect(page).toHaveURL(/\/book\/uk\/04-loops\//);
});

test('the same chapter is one click away in English', async ({ page }) => {
  await page.goto('/book/uk/04-loops/');
  await page.locator('#book-other').click();
  await expect(page).toHaveURL(/\/book\/en\/04-loops\//);
  await expect(page.locator('h1')).toHaveText('Loops');
});

test('the Studio Help menu opens the book', async ({ page }) => {
  await page.goto('/');
  await page.getByRole('menuitem', { name: 'Довідка' }).click();
  await expect(page.locator('.menu [data-command="book"]')).toContainText('Книга Yu');
});
```

- [ ] **Step 2: Run them**

Run (in `studio/`, with `YU_CHROMIUM` set and the preview on 4180):
`npm run build && YU_URL=http://localhost:4180 npx playwright test`
Expected: every test passes — the eleven Studio 2 checks and these six. A failure names the page;
fix the page, not the test's intent.

- [ ] **Step 3: Commit**

```bash
git add studio/tests/book.spec.ts
git -c user.email=y.dmytrenko14@gmail.com -c user.name="Yurii Dmytrenko" commit -m "Browser tests for the book: chapters, examples, theme, search" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 8: CI keeps the reference fresh, and the README points at the book

**Files:**
- Modify: `.github/workflows/ci.yml` (one step in the `studio` job)
- Modify: `README.md`

**Interfaces:**
- Consumes: `npm run build`, which regenerates `docs/book/<lang>/11-words.md`.

- [ ] **Step 1: Make CI fail on a stale reference**

In `.github/workflows/ci.yml`, in the `studio` job, right after the `npm run build` step, add:

```yaml
      - name: The book's reference matches the language
        run: git diff --exit-code ../docs/book
```

- [ ] **Step 2: Point the README at the book**

In `README.md`, after the paragraph that ends with `Нічого не треба встановлювати.` and its GIF,
add:

```markdown
**Книга Yu: [yu-lang.vercel.app/book](https://yu-lang.vercel.app/book/)** — десять розділів від
першої програми до трьох проєктів, українською та англійською; кожен приклад відкривається в
Студії одним натисканням. Ті самі тексти лежать у [`docs/book`](docs/book/README.md), і тест
проганяє кожен приклад із книги обома мовами.
```

In the section «Як це влаштовано», after the sentence about the library, add:

```markdown
Сторінки книги збирає `studio/scripts/build-book.mjs` із тих самих Markdown-файлів, а розділ
«Усі слова» — з бібліотеки ядра.
```

- [ ] **Step 3: Check**

Run: `cargo test --all`; in `studio/`: `npm run build && npm test && git diff --exit-code ../docs/book`
Expected: everything passes and the last command prints nothing — the reference is fresh.

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/ci.yml README.md
git -c user.email=y.dmytrenko14@gmail.com -c user.name="Yurii Dmytrenko" commit -m "CI keeps the book's reference fresh; the README links to the book" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 9: Ship the book

**Files:** none new.

- [ ] **Step 1: Verify the whole branch**

Run: `cargo fmt --all --check && cargo clippy --all-targets -- -D warnings && cargo test --all && cargo clippy -p yu-wasm --target wasm32-unknown-unknown -- -D warnings`; in `studio/`: `npm run build && npm test`, then with the preview running `YU_URL=http://localhost:4180 npx playwright test` (keep the exit code, do not pipe it into `tail`).
Expected: all green; note the numbers for the pull request.

- [ ] **Step 2: Open the pull request**

```bash
git push -u origin m5-book
gh pr create --base main --head m5-book --title "The Yu book: ten chapters in two languages, every example tested" --body-file "$BODY"
```

`$BODY` is a Markdown file written outside the repository. It says what the book holds, that a
Rust test runs every example and compares its output, that the reference chapter comes from the
core, and how it was tested. It ends with `🤖 Generated with [Claude Code](https://claude.com/claude-code)`.

- [ ] **Step 3: Watch CI, then merge**

Read the checks with the app's PR status (`ccd_pr get_status`), or wait with
`gh run watch <run id> --exit-status`. Expected: `rust (ubuntu-latest)`, `rust (windows-latest)`
and `studio` pass. A red check is fixed on the branch before anything is merged.

Then: `gh pr merge --merge`, `git checkout main`, `git pull`.

- [ ] **Step 4: Publish**

In `studio/`: `npm run build && vercel deploy dist --prod --yes --project yu-studio`
Expected: a new Production deployment. Check what the site really serves:
`curl -s https://yu-lang.vercel.app/book/uk/01-start/ | grep -o "<h1>[^<]*"`, and that
`https://yu-lang.vercel.app/sitemap.xml` and `/robots.txt` answer 200.

- [ ] **Step 5: Check the live book**

Run (in `studio/`): `YU_URL=https://yu-lang.vercel.app npx playwright test`
Expected: every test passes against the live site.

- [ ] **Step 6: Remember**

Update the `yu-language` memory: milestone 5a merged (PR number and merge commit), what the book
holds, and that milestone 5b is the release and the polish.
